/**
 * Global monetization kill switch (web).
 *
 * While `false`, everything is free: no Pro gates, no upsell modals, no
 * "Go Pro" entry points. Flip to `true` to restore the paid tier.
 */
export const MONETIZATION_ENABLED = false;
